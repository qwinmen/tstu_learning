эта самая последняя версия, в ней максимум обреза и более заточена под условия
задачи стаса, интеграл от 0 до 1 равен x+(x")*(x")
Система имеет вид
	var w0 = (-1.0) * t * t* t + 2.0 * t * t;
            var w1 = t * (t - 1.0) * t * t * (t - 1.0) * (t - 1.0) * Math.Pow(Math.E, t); 
            var w2 = t * (t - 1.0) * t * t * (t - 1.0) * (t - 1.0) * Math.Sin(t);
            return w0 + a1 * w1 + a2 * w2;